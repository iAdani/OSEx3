#include <iostream>
#include "BoundedQueue.h"

using namespace std;

// Reads the configuration file
void writeToScreen() {
    int done = 0, produced;

    while(done < 3) {
        if (produced == 2) produced = 0;


        produced++;
    }
}

int main() {

}
